import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tech.focux.mycoins',
  appName: 'mycoinsm',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
